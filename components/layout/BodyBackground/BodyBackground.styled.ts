import styled from "styled-components";

export const StyledBodyBackground = styled.div`
  position: fixed;
  height: calc(100vh - 8rem);
  margin-top: 8rem;
  width: 100%;
  z-index: -1;
  opacity: 0.3;
`;
