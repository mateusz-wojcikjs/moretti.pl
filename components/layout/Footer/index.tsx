import React from "react";
import { StyledFooter } from "./Footer.styled";

const Footer = () => {
  return (
    <footer>
      <StyledFooter>
        <div>Stopka</div>
      </StyledFooter>
    </footer>
  );
};

export default Footer;
